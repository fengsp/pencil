initSidebarItems({"enum":[["Error","An error type for the `formdata` crate."]],"fn":[["generate_boundary","Generate a valid multipart boundary, statistically unlikely to be found within the content of the parts."],["read_formdata","Parse MIME `multipart/form-data` information from a stream as a `FormData`."],["write_formdata","Stream out `multipart/form-data` body content matching the passed in `formdata`.  This does not stream out headers, so the caller must stream those out before calling write_formdata()."],["write_formdata_chunked","Stream out `multipart/form-data` body content matching the passed in `formdata` as Transfer-Encoding: Chunked.  This does not stream out headers, so the caller must stream those out before calling write_formdata()."]],"struct":[["FilePart","A file that is to be inserted into a `multipart/*` or alternatively an uploaded file that was received as part of `multipart/*` parsing."],["FormData","The extracted text fields and uploaded files from a `multipart/form-data` request."]]});